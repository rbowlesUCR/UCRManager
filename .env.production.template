# UCRManager Production Environment Template
# Copy this file and fill in the values during deployment

# Database Configuration
DATABASE_URL=postgresql://postgres:CHANGE_THIS_PASSWORD@localhost:5432/ucrmanager

# Session Secret (Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))")
SESSION_SECRET=GENERATE_64_CHAR_RANDOM_STRING

# Encryption Key for ConnectWise/3CX credentials (Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
ENCRYPTION_KEY=GENERATE_32_BYTE_BASE64_KEY

# Application Environment
NODE_ENV=production

# Application Port (443 for HTTPS)
PORT=443

# Optional: Microsoft Graph Integration (if using)
# GRAPH_CLIENT_ID=
# GRAPH_CLIENT_SECRET=
# GRAPH_TENANT_ID=

# Optional: SSL Certificate Paths (if using file-based certs)
# SSL_CERT_PATH=C:\Certificates\server.crt
# SSL_KEY_PATH=C:\Certificates\server.key
